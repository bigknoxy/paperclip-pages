Paperclip — workspace starter

This repository contains a small scaffold created and maintained by agent `6d753339-1dbd-459d-a1ba-781247680a61` (CEO) to bootstrap development.

What is here
- `index.html` — landing page and React mount point
- CI / Status
- ![CI](https://img.shields.io/badge/ci-pending-lightgrey) <!-- replace with real Actions badge after push -->
- ![Tests](https://img.shields.io/badge/tests-passing-brightgreen) <!-- replace with Actions tests badge -->
- ![Pages](https://img.shields.io/badge/pages-deploying-lightgrey) <!-- replace with Pages badge once enabled -->
- `src/` — React demo, small utility, and tests
- `dist/` — built site (generated by `npm run build`)
- `tasks/todo.md` — short task checklist
- `docs/MEMORY.md` — decision log
- `.github/workflows/` — CI for tests and Pages deploy

Quick start (local)
1. Install dependencies: `npm install`
2. Run dev server: `npm run dev`
3. Run tests: `npm test`
4. Build for production: `npm run build` (output in `dist/`)

Notes
- The project uses Vite + React for the demo UI and Vitest for tests.
- GitHub Actions are configured to run tests and deploy the `dist/` site to Pages on `main`.

Demo notes
- The React demo includes a small interactive example on the landing page: a "Say hi" button and a simple "note" input. Type a note and click "Save note" to store it in browser localStorage (or a test fallback in non-browser environments). Use this to verify the interactive pipeline locally.

If you'd like me to prepare a PR with these changes, or to open specific issues and assign them, tell me which step to take next.

Ready-to-push notes
- A local branch `chore/scaffold` contains the scaffold and is ready to push.
- To push and open a PR manually:

  ```bash
  git push -u origin chore/scaffold
  # then create PR with gh (GitHub CLI):
  gh pr create --title "chore: initial scaffold — demo, tests, CI, docs" --body-file .github/PR_BODY.md
  ```

I can push and open the PR for you if you reply: "Yes, push and open PR".
